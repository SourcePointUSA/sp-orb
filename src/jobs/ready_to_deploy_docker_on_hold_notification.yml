description: >
  Deployment on hold message

executor: pl_standard_executor

steps:
  - sp_slack_notify:
      header: "Deployment (staging/prod) awaiting approval :raised_hand:"
      link_to_workflow: true